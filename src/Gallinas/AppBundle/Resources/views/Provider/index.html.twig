{% extends '::layout_list.html.twig' %}
{% block title_wrapper %}Lista de Proveedorxs{% endblock %}
{% block main -%}
    <div class="panel panel-default">
        <div class="panel-heading">
            Listado
        </div>
        <div class="panel-body">
            <div class="table-responsive">
                <table id="table_list"  class="table table-striped table-bordered table-hover dataTable no-footer"  aria-describedby="dataTables-example_info">
                    <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Web</th>
                        <th>Acciones</th>
                    </tr>
                    </thead>
                    <tfoot>
                    <tr role="row">
                        <th>Nombre</th>
                        <th>Web</th>
                        <th>Acciones</th>
                    </tr>
                    </tfoot>
                    <tbody>
                    {% for entity in entities %}
                        <tr class="gradeA">
                            <td><a href="{{ path('provider_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                            <td>{{ entity.web }}</td>
                            <td>
                                <ul>
                                    <li>
                                        <a href="{{ path('provider_show', { 'id': entity.id }) }}">mostrar</a>
                                    </li>
                                    <li>
                                        <a href="{{ path('provider_edit', { 'id': entity.id }) }}">editar</a>
                                    </li>
                                </ul>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <ul>
        <li>
            <a href="{{ path('provider_new') }}">
                Añadir nuevx proveedorx
            </a>
        </li>
    </ul>
{% endblock %}