{% extends '::layout_crud.html.twig' %}

    {% block title_wrapper %}Añadir {% endblock %}
    {% block main -%}
        <div class="panel panel-default">

            <div class="panel-body">
                {{ form_start(form, {'action': path('variety_create'),'method': 'post','attr': {'role': 'form' }}) }}
                <div class='row'>
                    <div class="col-lg-6">
                        <div class="form-group">
                            {{ form_errors(form.name) }}
                            <label>{{ form_label(form.name) }}</label>
                            {{ form_widget(form.name,{'attr':{'class':'form-control','placeholder':'Nombre' }}) }}
                        </div>
                        <div class="form-group">
                            {{ form_errors(form.crop) }}
                            <label>{{ form_label(form.crop) }}</label>
                            {{ form_widget(form.crop,{'attr':{'class':'form-control','placeholder':'Cultivo' }}) }}
                        </div>
                    </div>
                </div>
                {{ form_widget(form.submit,{'attr':{'class':'btn btn-default'}}) }}
                {{ form_rest(form) }}
                {{ form_end(form, {'render_rest': false}) }}
                <ul class="record_actions">
                    <li>
                        <a href="{{ path('variety') }}">
                            Back to the list
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    {% endblock %}
{% block more_js %}{% endblock %}