{% extends '::layout_list.html.twig' %}
{% block title_wrapper %}Lista de Eventos{% endblock %}
{% block main -%}
<div class="panel panel-default">
    <div class="panel-heading">
        Listado
    </div>
    <div class="panel-body">
        <div class="table-responsive">
            <table id="table_list" class="table table-striped table-bordered table-hover dataTable no-footer"
                   aria-describedby="dataTables-example_info">
        <thead>
            <tr>
                <th>Title</th>
                <th>Start_date</th>
                <th>End_date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('event_show', { 'id': entity.id }) }}">{{ entity.title }}</a></td>
                <td>{% if entity.startdate %}{{ entity.startdate|localizeddate('long','none','es') }}{% endif %}</td>
                <td>{% if entity.enddate %}{{ entity.enddate|localizeddate('long','none','es') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('event_show', { 'id': entity.id }) }}">mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('event_edit', { 'id': entity.id }) }}">editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        </div>
    </div>
</div>

    <ul>
        <li>
            <a href="{{ path('event_new') }}">
                Añadir evento
            </a>
        </li>
    </ul>
{% endblock %}
