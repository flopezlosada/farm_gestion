{% extends '::layout_list.html.twig' %}
{% block title_wrapper %}Lista de Clientxs{% endblock %}
{% block main -%}
    <div class="panel panel-default">
        <div class="panel-heading">
            Listado
        </div>
        <div class="panel-body">
            <div class="table-responsive">
                <table id="table_list" class="table table-striped table-bordered table-hover dataTable no-footer"
                       aria-describedby="dataTables-example_info">

                    <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>¿Compra con frecuencia?</th>
                        <th>Frecuencia de compra</th>
                        <th>Acciones</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for entity in entities %}
                        <tr>
                            <td><a href="{{ path('purchaser_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                            <td>
                                {% if entity.regular==1 %}
                                    Sí
                                {% else %}
                                    No
                                {% endif %}
                            </td>
                            <td>
                                {% if entity.regular==1 %}
                                    {{ entity.getOftenBuyingEggs }} días
                                {% else %}
                                    ---
                                {% endif %}
                            </td>
                            <td>
                                <ul>
                                    <li>
                                        <a href="{{ path('purchaser_show', { 'id': entity.id }) }}">show</a>
                                    </li>
                                    <li>
                                        <a href="{{ path('purchaser_edit', { 'id': entity.id }) }}">edit</a>
                                    </li>
                                </ul>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <ul>
        <li>
            <a href="{{ path('purchaser_new') }}">
                Añadir clientx
            </a>
        </li>
    </ul>
{% endblock %}