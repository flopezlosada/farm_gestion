{% block extends %}
    {{ "{% extends '::layout_crud.html.twig' %}" }}
{% endblock extends %}

{% block body %}
    {{ '{% block title_wrapper %}Editar {% endblock %}' }}
    {{ "{% block main -%}" }}
    <div class="panel panel-default">
        <div class="panel-heading">

        </div>
        <div class="panel-body">
            {{ "{{ form_start(edit_form, {'action': path('"~route_name_prefix ~ "_update',{'id':entity.id}), 'method': 'put','attr': {'role': 'form' }}) }}" }}
            {{ "{{ form_errors(edit_form) }}" }}
            <div class="row">
            </div>
            {{ "{{ form_widget(edit_form.submit,{'attr':{'class':'btn btn-default' }}) }}" }}
            {{ "{{ form_rest(edit_form) }}" }}
            {{ "{{ form_end(edit_form, {'render_rest': false}) }}" }}
            {{ '{{ form(edit_form) }}' }}
        </div>
    </div>
    {% set hide_edit, hide_delete = true, false %}
    {% include 'crud/views/others/record_actions.html.twig.twig' %}
    {{ "{% endblock %}" }}
{% endblock body %}
