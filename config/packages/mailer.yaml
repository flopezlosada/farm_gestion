framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        # Remitente por defecto para todos los emails de la app. En local va a
        # Mailpit (no se envía a nadie real); en producción se ajusta a la
        # dirección operativa de la asociación.
        headers:
            From: 'CSA Vega de Jarama <noreply@csavegadejarama.org>'
