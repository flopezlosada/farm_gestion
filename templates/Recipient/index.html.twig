{# ============================================================
 # Listado de agraciadxs (Recipient) — receptorxs de regalos.
 # ============================================================ #}

{% extends '_layouts/csa_shell.html.twig' %}
{% import '_macros/csa.html.twig' as csa %}

{% block title %}Agraciadxs{% endblock %}

{% block csa_crumbs %}
    <a href="{{ path('dashboard') }}">Inicio</a>
    <span class="csa-topbar__crumbs-sep">/</span>
    <span class="csa-topbar__crumb-active">Agraciadxs</span>
{% endblock %}

{% block csa_main %}

    {{ csa.page_header(
        'Comercio · Regalos',
        'Agraciadxs',
        'Personas a las que se les regalan productos (sobre todo huevos). Si reciben con frecuencia, se anota la cadencia.'
    ) }}

    <div class="csa-row-between" style="margin-bottom: var(--csa-sp-5);">
        <div></div>
        <div class="csa-row">
            {{ csa.btn('Nuevx agraciadx', { variant: 'primary', href: path('recipient_new'), icon: 'fa-plus' }) }}
        </div>
    </div>

    <div class="csa-table-wrap">
        <table class="csa-table" data-paginate>
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th class="csa-table__col-tight">¿Regular?</th>
                    <th class="csa-table__col-tight">Frecuencia</th>
                    <th class="csa-table__actions">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for entity in entities %}
                    <tr>
                        <td>
                            <a class="csa-table__primary" href="{{ path('recipient_show', { id: entity.id }) }}">{{ entity.name }}</a>
                        </td>
                        <td class="csa-table__col-tight">
                            {% if entity.regular == 1 %}Sí{% else %}—{% endif %}
                        </td>
                        <td class="csa-table__col-tight">
                            {% if entity.regular == 1 %}cada {{ entity.getOftenBuyingEggs }} días{% else %}—{% endif %}
                        </td>
                        <td class="csa-table__actions">
                            <a href="{{ path('recipient_show', { id: entity.id }) }}" title="Ver" aria-label="Ver"><i class="fa fa-eye"></i></a>
                            <a href="{{ path('recipient_edit', { id: entity.id }) }}" title="Editar" aria-label="Editar"><i class="fa fa-pencil"></i></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="csa-muted" style="text-align: center; padding: var(--csa-sp-8);">
                            Aún no hay agraciadxs registradxs.
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
