{# ============================================================
 # Listado de compras (Purchase).
 # ============================================================ #}

{% extends '_layouts/csa_shell.html.twig' %}
{% import '_macros/csa.html.twig' as csa %}

{% block title %}Compras{% endblock %}

{% block csa_crumbs %}
    <a href="{{ path('dashboard') }}">Inicio</a>
    <span class="csa-topbar__crumbs-sep">/</span>
    <span class="csa-topbar__crumb-active">Compras</span>
{% endblock %}

{% block csa_main %}

    {{ csa.page_header(
        'Comercio · Compras',
        'Compras',
        'Registro de productos comprados a proveedorxs. Cada compra indica si está pagada o pendiente.'
    ) }}

    <div class="csa-row-between" style="margin-bottom: var(--csa-sp-5);">
        <div></div>
        <div class="csa-row">
            {{ csa.btn('Nueva compra', { variant: 'primary', href: path('purchase_new'), icon: 'fa-plus' }) }}
        </div>
    </div>

    <div class="csa-table-wrap">
        <table class="csa-table" data-paginate>
            <thead>
                <tr>
                    <th class="csa-table__col-tight">Fecha</th>
                    <th>Proveedorx</th>
                    <th>Producto</th>
                    <th class="csa-table__col-tight">Cantidad</th>
                    <th class="csa-table__col-tight">Total</th>
                    <th class="csa-table__col-tight">¿Pagada?</th>
                    <th class="csa-table__actions">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for entity in entities %}
                    <tr>
                        <td class="csa-table__col-tight">
                            {% if entity.purchasedate %}
                                <a class="csa-table__primary" href="{{ path('purchase_show', { id: entity.id }) }}">{{ entity.purchasedate|format_date('medium', locale='es') }}</a>
                            {% else %}—{% endif %}
                        </td>
                        <td>
                            {% if entity.Provider %}
                                <a href="{{ path('provider_show', { id: entity.Provider.id }) }}">{{ entity.provider }}</a>
                            {% else %}—{% endif %}
                        </td>
                        <td>{% if entity.product %}{{ entity.product }}{% else %}—{% endif %}</td>
                        <td class="csa-table__col-tight">{{ entity.amount }}{% if entity.Unity %} {{ entity.Unity }}{% endif %}</td>
                        <td class="csa-table__col-tight">{{ entity.totalprice }} €</td>
                        <td class="csa-table__col-tight">
                            {% if entity.paid %}
                                <span class="csa-badge csa-badge--green">Sí</span>
                            {% else %}
                                <span class="csa-badge csa-badge--terracotta">No</span>
                            {% endif %}
                        </td>
                        <td class="csa-table__actions">
                            <a href="{{ path('purchase_show', { id: entity.id }) }}" title="Ver" aria-label="Ver"><i class="fa fa-eye"></i></a>
                            <a href="{{ path('purchase_edit', { id: entity.id }) }}" title="Editar" aria-label="Editar"><i class="fa fa-pencil"></i></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7" class="csa-muted" style="text-align: center; padding: var(--csa-sp-8);">
                            Aún no hay compras registradas.
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
