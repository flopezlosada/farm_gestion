{# ============================================================
 # Listado de destinos de animales sacrificados (FowlDestination).
 # ============================================================ #}

{% extends '_layouts/csa_shell.html.twig' %}
{% import '_macros/csa.html.twig' as csa %}

{% block title %}Destinos de animales{% endblock %}

{% block csa_crumbs %}
    <a href="{{ path('dashboard') }}">Inicio</a>
    <span class="csa-topbar__crumbs-sep">/</span>
    <span class="csa-topbar__crumb-active">Destinos de animales</span>
{% endblock %}

{% block csa_main %}

    {{ csa.page_header(
        'Granja · Animales',
        'Destinos de animales',
        'Catálogo de destinos posibles para un animal sacrificado: vendido, regalado, consumido por nosotrxs, etc.'
    ) }}

    <div class="csa-row-between" style="margin-bottom: var(--csa-sp-5);">
        <div></div>
        <div class="csa-row">
            {{ csa.btn('Nuevo destino', { variant: 'primary', href: path('fowldestination_new'), icon: 'fa-plus' }) }}
        </div>
    </div>

    <div class="csa-table-wrap">
        <table class="csa-table" data-paginate>
            <thead>
                <tr>
                    <th>Destino</th>
                    <th class="csa-table__actions">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for entity in entities %}
                    <tr>
                        <td>
                            <a class="csa-table__primary" href="{{ path('fowldestination_show', { id: entity.id }) }}">{{ entity.name }}</a>
                        </td>
                        <td class="csa-table__actions">
                            <a href="{{ path('fowldestination_show', { id: entity.id }) }}" title="Ver" aria-label="Ver"><i class="fa fa-eye"></i></a>
                            <a href="{{ path('fowldestination_edit', { id: entity.id }) }}" title="Editar" aria-label="Editar"><i class="fa fa-pencil"></i></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2" class="csa-muted" style="text-align: center; padding: var(--csa-sp-8);">
                            No hay destinos registrados.
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
