{% extends 'base.html.twig' %}

{% block title %}WeeklyBasketGroup{% endblock %}

{% block body %}
    <h1>WeeklyBasketGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ weekly_basket_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ weekly_basket_group.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ weekly_basket_group.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('weekly_basket_group_index') }}">back to list</a>

    <a href="{{ path('weekly_basket_group_edit', {'id': weekly_basket_group.id}) }}">edit</a>

    {{ include('weekly_basket_group/_delete_form.html.twig') }}
{% endblock %}
