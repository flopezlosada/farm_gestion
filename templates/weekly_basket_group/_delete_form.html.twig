<form method="post" action="{{ path('weekly_basket_group_delete', {'id': weekly_basket_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weekly_basket_group.id) }}">
    <button class="btn btn-outline btn-danger">Eliminar</button>
</form>
