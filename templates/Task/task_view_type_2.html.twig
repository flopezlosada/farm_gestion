{# ============================================================
 # Tarjeta de tarea tipo 2 (fecha fija) — partial.
 # ============================================================ #}

<div class="csa-task-card csa-task-card--terracotta" style="background: var(--csa-terracotta-soft); border-left: 4px solid var(--csa-terracotta); padding: var(--csa-sp-5); border-radius: var(--csa-radius-md); margin-bottom: var(--csa-sp-4);">
    <h4 style="margin-top: 0;">{{ task.title }} <span class="csa-soft" style="font-weight: normal; font-size: 0.85em;">· Fecha fija</span></h4>
    {% if task.realDate %}
        <p><strong>Finalizada el:</strong> {{ task.realDate|format_date('long', locale='es') }}</p>
    {% else %}
        <p><strong>Fecha prevista:</strong> {{ task.expectedDate|format_date('long', locale='es') }}</p>
    {% endif %}
    {% if task.users|length > 0 %}
        <p><strong>Asignada a:</strong> {{ task.listUsers }}</p>
    {% endif %}
    <div>{{ task.content|raw }}</div>
    {% include 'Task/record_actions.html.twig' with { task: task } %}
</div>
