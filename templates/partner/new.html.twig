{# ============================================================
 # Nuevx socix — shell csa.
 # ============================================================ #}

{% extends '_layouts/csa_shell.html.twig' %}
{% import '_macros/csa.html.twig' as csa %}
{% form_theme form 'form_div_layout.html.twig' %}

{% block title %}Nuevx socix{% endblock %}

{% block csa_crumbs %}
    <a href="{{ path('dashboard') }}">Inicio</a>
    <span class="csa-topbar__crumbs-sep">/</span>
    <a href="{{ path('partner_index') }}">Socixs</a>
    <span class="csa-topbar__crumbs-sep">/</span>
    <span class="csa-topbar__crumb-active">Nuevx</span>
{% endblock %}

{% block csa_main %}

    {{ csa.page_header(
        'Socixs · Alta',
        'Nuevx socix',
        'Sólo datos de persona. La cesta y el punto de recogida se asignan después desde la ficha.'
    ) }}

    {{ form_start(form, { attr: { role: 'form', class: 'csa-form-narrow', enctype: 'multipart/form-data' } }) }}

        <div class="csa-stack-lg">

            {% embed '_components/csa_form_section.html.twig' with { num: '1', title: 'Identidad', subtitle: 'Nombre, apellidos y DNI.' } %}
                {% block body %}
                    {% import '_macros/csa.html.twig' as csa %}
                    <div class="csa-form-grid">
                        {{ csa.field(form.name, 'Nombre') }}
                        {{ csa.field(form.surname, 'Apellidos') }}
                        {{ csa.field(form.displayName, 'Nombre de reparto', { full: true, hint: 'cómo se conoce a la familia en el listado de cestas · vacío = nombre y apellidos' }) }}
                        {{ csa.field(form.DNI, 'DNI') }}
                        {{ csa.field(form.inscription_date, 'Fecha de inscripción', { attr: { class: 'csa-input datepicker' } }) }}
                    </div>
                {% endblock %}
            {% endembed %}

            {% embed '_components/csa_form_section.html.twig' with { num: '2', title: 'Contacto', subtitle: 'Teléfono y email.' } %}
                {% block body %}
                    {% import '_macros/csa.html.twig' as csa %}
                    <div class="csa-form-grid">
                        {{ csa.field(form.celular, 'Teléfono') }}
                        {{ csa.field(form.email, 'Email') }}
                    </div>
                {% endblock %}
            {% endembed %}

            {% embed '_components/csa_form_section.html.twig' with { num: '3', title: 'Dirección', subtitle: 'Localización postal.' } %}
                {% block body %}
                    {% import '_macros/csa.html.twig' as csa %}
                    <div class="csa-form-grid">
                        {{ csa.field(form.address, 'Dirección', { full: true }) }}
                        {{ csa.field(form.state, 'Provincia') }}
                        {{ csa.field(form.city, 'Municipio') }}
                    </div>
                {% endblock %}
            {% endembed %}

            {% embed '_components/csa_form_section.html.twig' with { num: '4', title: 'Cuota y preferencias', subtitle: 'Forma de pago de la cuota y consumo.' } %}
                {% block body %}
                    {% import '_macros/csa.html.twig' as csa %}
                    <div class="csa-form-grid">
                        {{ csa.field(form.share_payment, 'Forma de pago de la cuota') }}
                        {{ csa.field(form.eat_meat, '¿Consume cárnicos?', { full: true }) }}
                        {{ csa.field(form.registration_form_file, 'Ficha de socix', { full: true, hint: 'opcional' }) }}
                    </div>
                    <div style="margin-top: var(--csa-sp-4);">
                        {{ csa.callout(
                            'La cesta y el punto de recogida no se eligen aquí: tras crear la ficha, usa «Añadir cesta» para asignarlos.',
                            'info', 'shopping-basket'
                        ) }}
                    </div>
                {% endblock %}
            {% endembed %}

            <div class="csa-row-between" style="padding-top: var(--csa-sp-5); border-top: 1px solid var(--csa-rule);">
                <div></div>
                <div class="csa-row">
                    {{ csa.btn('Cancelar', { variant: 'ghost', href: path('partner_index') }) }}
                    <button type="submit" class="csa-btn csa-btn--primary">Crear socix</button>
                </div>
            </div>

        </div>

        {{ form_rest(form) }}
    {{ form_end(form, { render_rest: false }) }}

{% endblock %}

{% block more_js %}
    {{ parent() }}
    <script>
        document.querySelectorAll('.datepicker, .datepicker2, .datepicker3')
            .forEach(function (el) { el.type = 'date'; });

        // AJAX dependiente provincia → municipio
        $('#partner_city').html('');
        $('#partner_city').prop('required', true);
        $('#partner_state').change(function () {
            $.ajax({
                type: 'get',
                url: '{{ path("select_city") }}',
                data: { state_id: $(this).val() },
                success: function (data) {
                    $('#partner_city').html(data);
                }
            });
        });
    </script>
{% endblock %}
